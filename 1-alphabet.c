#include <stdio.h>
/* description */

void print_alphabet(void);
	int main(void)
{
	int ch;
	    for ( ch = 'a' ; ch <= 'z' ; ch++ )
	    {
		    putchar (ch);
	    }
	            putchar ('\n');
	    return (0);
}
